# AgentImageViewer 开发流程（Flutter + Filter Pipeline 架构）

依据《AgentImageViewer 设计文档 完整版 v2》编制，并按以下两项决定调整：
- **运行底座**：弃用 Electron + Capacitor，改用 **Flutter**，参考 `AgentVideoPlayer_Flutter` 工程的分层与目录结构，四端（Windows/Linux/Android/iOS）共用一份 Dart 代码。
- **核心架构**：图像处理统一为 **Filter Pipeline（滤镜流水线）**——解码 → 处理节点 → 渲染/导出，浏览、编辑、AI 处理都是同一条管道的不同组合。

---

## 1. 架构总纲：Filter Pipeline

### 1.1 管道模型

图像从源到呈现/落盘，统一经过三级流水线：

```
Source（源节点） → Filter（处理节点，N 个，有序） → Sink（输出节点）
```

- **Source**：负责把文件变成像素。文件路径 / 内存字节 → `ui.Image`（`instantiateImageCodec` 异步解码），支持降采样解码（targetWidth/Height）与分块解码（超大图 tile，配合 Rect 区域请求）。动图 Source 输出帧序列。
- **Filter**：所有像素变换都是节点，每个节点是 `(input Canvas/Image, params) → output` 的纯函数式变换，可组合、可插拔、可求值。对应设计书编辑工具清单（表 2-3）：
  - 几何：rotate / flip / crop
  - 调整：brightness / contrast / saturation / temperature / sharpness / vignette（ColorFilter 或 FragmentShader 实现，优先 ColorFilter，不够再上 shader）
  - 滤镜预设：等价于一组调整节点的组合（黑白 = saturate 0 + contrast +0.1 …）
  - 标注：text / arrow / rect / ellipse / mosaic / doodle（矢量绘制节点，坐标存 0–1 相对比例）
  - 尺寸：resize（双线性）/ canvas 扩展
- **Sink**：
  - `RenderSink`：画到 Widget 画布（浏览视图/编辑预览），实时重算；
  - `ExportSink`：`toImage` → `png/jpg/webp` 编码落盘，导出时才执行。

### 1.2 与设计书概念的映射

| 设计书概念 | Filter Pipeline 中的形态 |
|---|---|
| 编辑操作栈（3.4 节 JSON 数组） | 就是管道定义的持久化形式：每个 op 一个 Filter 节点，画布 = 逐节点求值 |
| 非破坏性编辑 | 只存节点参数，不存像素；任何 Sink 重算即可 |
| AI 指令式处理（ai_edit 工具） | Agent 把自然语言**编译为节点序列**追加进管道（如「日系冷调」= temperature −0.15 + saturation −0.1 + brightness +0.05）；生成式能力（背景替换等）作为特殊 AI 节点，执行时才调云端 |
| 浏览视图 | Pipeline 只含 Source + RenderSink，零 Filter，即最快路径 |
| 撤销/重做/跳转历史 | 管道节点序列的增删与截断，天然支持 |

**性能红线**：浏览态（零 Filter）不得经过任何 Filter 框架开销，Source→RenderSink 直连；Filter 链只在编辑态与导出态求值。调整类滑杆实时预览时，被调参数所在节点之后的节点才重算，之前的节点结果缓存复用。

### 1.3 模块划分（对照 AgentVideoPlayer_Flutter 目录）

```
lib/
├── core/                    # 纯 Dart，不 import flutter UI 之外的平台 API
│   ├── pipeline/
│   │   ├── source.dart      # 解码、降采样、tile、动图帧、EXIF
│   │   ├── node.dart        # FilterNode 基类、参数序列化（↔ 3.4 节 JSON）
│   │   ├── pipeline.dart    # 节点序列求值、增量重算、历史栈
│   │   └── sinks.dart       # 渲染求值 / 导出编码（encoder 依赖平台通道的放 platform/）
│   ├── scanner.dart         # 图库递归扫描、缩略图任务（参考工程已有同名模块）
│   ├── db/                  # library_store / edit_stack / settings（参考工程用 Drift，沿用）
│   ├── ai/                  # ai_client（OpenAI 兼容）、agent 工具注册、串行队列（参考 ai/ 现有结构）
│   └── models/
├── ui/                      # 图库/浏览/编辑/AI 面板/设置（深色主题，表 4-1 色板）
└── platform/                # 平台通道：文件关联、回收站、托盘、壁纸、分享/相册（移动）
```

与参考工程的对应：`core/scanner.dart`、`core/db/`、`core/ai/`、`core/library_store` 直接对齐或移植；参考工程的 `player/` 换成本项目的 `pipeline/`。

## 2. 阶段总览

| 阶段 | 对应版本 | 内容 | 出口条件 |
|---|---|---|---|
| S0 工程奠基 | — | Flutter 工程初始化、pipeline 骨架、深色主题壳 | 空管道跑通：打开一张图显示出来 |
| S1 Source 层与浏览 | v0.1 | 解码/降采样/tile/缓存 + 浏览视图全套交互 | 双击打开即看即翻，性能达 3.3 节口径 |
| S2 Filter 节点与编辑 | v0.1 | 节点框架 + 五组节点 + 导出 Sink + 图库 | 编辑可撤销可导出，万张图库不卡 |
| S3 v0.1 发布 | v0.1 | Windows 打包（MSIX/便携 exe）、回归 | 安装包交付 |
| S4 AI Agent | v0.2 | ai/agent/queue + AI 面板 + 指令→节点编译 | 批量任务可容错、可确认、可回滚 |
| S5 桌面双端与集成 | v0.3 | Linux 适配、文件关联、托盘、性能专项 | 双击图片直达浏览，万张图库不卡顿 |
| S6 移动端 | v0.4 | Android/iOS：手势、相册、分享、后台策略 | 手势完整，核心功能与桌面一致 |
| S7 全平台收尾 | v1.0 | iOS 上架、四端一致性收尾 | 四端体验一致 |

相比 Electron 方案的简化：**不再需要 S6 之前单独的 Capacitor 层**——Flutter 四端一套工程（参考工程 windows/android/ios/linux 目录已就位），S6 只做移动端交互与系统能力适配。

## 3. 仓库结构与分支规范

- 直接在参考工程结构上起仓库：`lib/core`、`lib/ui`、`lib/platform`，`test/` 放 core 层单测，`windows/ android/ ios/ linux/` 平台工程。
- 分支：`main` 可运行；功能走 `feat/xxx`；**pipeline/node.dart 的节点参数结构是全项目最重要的契约**，改动必须兼容旧编辑栈 JSON 或提供迁移，并在 PR 中注明。
- CI：`flutter analyze` + core 层单测 + `flutter build windows` smoke。

## 4. 单个功能的标准开发循环

1. **对齐**：从设计书摘出功能章节（功能第 2 章、界面第 4 章、交互第 5 章），确认验收口径。
2. **节点先行**：能表达为 Filter 节点的功能，先定节点名与参数 JSON（与 3.4 节操作栈格式互认），再写实现。
3. **实现**：core 层纯 Dart 配单测（pipeline 求值可在 VM 测试，用 `flutter test`）；UI 层联调；平台能力走 `platform/` 通道，core 禁止出现平台分支。
4. **自测**：性能口径（3.3 节）+ 交互清单（表 5-1/5-2/5-3/5-4）。
5. **合并**：PR 附验收截图/录屏。

## 5. 各阶段要点

### S0 工程奠基（约 1 周）
- 以 AgentVideoPlayer_Flutter 为模板建工程，删播放器相关，保留 core/db、core/scanner 骨架与深色主题壳（表 4-1 色板直接落成 ThemeData 常量）。
- 实现 `Pipeline` 骨架：Source（单图解码）→ RenderSink 显示。窗口管理用 `window_manager`（无边框、自绘标题栏、最小 960×600），单实例用 `windows_single_instance` 或平台通道。
- db 沿用参考工程的 Drift 方案（JSON 落盘要求改为 Drift 表或保留 JSON 原子写均可，但必须原子）。

### S1 Source 层与浏览（v0.1 核心，2–3 周）
- 顺序：异步解码与降采样 → 缩放平移/翻页/键盘（表 5-1、5-3）→ tile 分块（放大超 100% 按可见区域请求）→ scanner 扫描 + 缩略图磁盘缓存（路径+mtime+尺寸哈希）→ LRU 内存缓存（桌面 512 MB）。
- 动图：GIF/WebP 帧序列 Source，暂停即停取帧；EXIF 自研轻量解析。
- 每步用亿像素样图与万张文件夹两个标准用例实测，内存超限即修，不后补。

### S2 Filter 节点与编辑（v0.1 收口，2–3 周）
- 先做节点框架：Node 基类、序列求值、增量重算、历史栈（编辑栈 JSON ↔ 3.4 节格式）。
- 节点按组填充：几何 → 调整（滑杆实时预览）→ 标注 → 滤镜预设 → 尺寸；坐标一律 0–1 相对比例。
- ExportSink：PNG/JPG/WebP 编码（经平台通道或纯 Dart encoder），另存副本 / 覆盖（确认 + .bak）。
- 图库：网格、搜索、标签/收藏（虚拟操作）、空/加载/错误态。

### S3 v0.1 发布
- Windows 打包：`flutter build windows` + MSIX 或便携 zip；全量过操作/快捷键回归清单；tag v0.1。

### S4 AI Agent（v0.2，3–4 周）
- 顺序：ai_client（OpenAI 兼容、base64 图片消息、指数退避）→ 串行任务队列逐项容错 → 七个工具注册（表 2-4）→ AI 面板（会话流、任务卡片、确认卡片）。
- **ai_edit 的核心是指令→节点编译器**：让模型输出节点序列 JSON，本地校验节点名与参数合法后才入管道；生成式节点（背景替换、对象消除）调云端前明示上传内容。
- 红线：写操作默认虚拟、真实写必须经确认卡片、批量结束汇报成败清单。表 5-5 五条指令作为验收基准。

### S5 桌面双端与系统集成（v0.3，2–3 周）
- Linux 适配清理（路径、窗口、打包 AppImage/deb）。
- 文件关联：Windows 写 HKCU 注册表（平台通道实现，无需管理员）、单实例转发路径、取消关联清理；Linux .desktop MIME。
- 托盘 + AI 完成通知、设为壁纸；性能专项收口。

### S6 移动端（v0.4，3–4 周）
- Android 先行：手势（表 5-2：双指缩放、滑动翻页跟手回弹、长按菜单、上滑返回）、底部标签栏、两列网格、safe-area。
- 相册/分享接收（platform 通道）、权限被拒降级文件选择器。
- 移动策略：缓存 256 MB、后台暂停解码与 AI 队列、数据导出/导入迁移。iOS 随后，仅处理平台差异。

### S7 全平台收尾（v1.0）
- iOS TestFlight → 上架；四端过交互与性能清单；HEIC/AVIF 缺失检测与引导。

## 6. 贯穿全程的机制

- **测试**：core 层（pipeline 求值、scanner、db、ai、agent 工具映射）单测必做；手测清单 = 表 5-1/5-2/5-3/5-4 + AI 用例表 5-5，每个里程碑前全量过一遍。
- **性能基准**：亿像素样图 + 万张文件夹两个标准用例从 S1 用到 S7；浏览态零 Filter 开销、调整滑杆增量重算两条性能红线进 PR 检查项。
- **风险前置**（表 8-1 对照 Flutter 的修订）：超大图内存（S1 起，tile + LRU）；WebView 差异风险**消失**（Flutter 自绘引擎，无系统 WebView 依赖——这是换 Flutter 的直接收益）；Impeller/Skia 渲染差异改为关注点（老 Android 机型降级验证）；AI 误操作三重防护（S4 起）；AI 服务商差异（ai_client 错误归一）。
- **契约文档**：节点参数 JSON、Drift 表结构、平台通道 API 三份契约变更必须同步 docs/。
- **里程碑评审**：对照表 7-1 交付标准逐条验收，未达标项明确修复或顺延。

## 7. 排期参考（单人全职估算，可按人数缩放）

| 里程碑 | 周期 | 累计 |
|---|---|---|
| S0 奠基 | 1 周 | 1 周 |
| v0.1 MVP | 5–6 周 | 6–7 周 |
| v0.2 AI Agent | 3–4 周 | 10–11 周 |
| v0.3 桌面双端 | 2–3 周 | 13–14 周 |
| v0.4 移动端 | 3–4 周 | 17–18 周 |
| v1.0 全平台 | 3–4 周 | 20–22 周 |

（Flutter 方案比 Electron+Capacitor 省掉一整套桥接层，移动端阶段缩短约 1 周。）
