# AgentImageViewer 交接文档

> 最后更新：2026-09-08 ｜ 当前 HEAD：见 git log ｜ 版本：v0.4.0-alpha.3+
> 交接目的：让下一个 agent/开发者能直接继续开发与排障，避免重复踩坑。

---

## 1. 项目是什么

跨平台 AI 图片浏览器（Windows/Linux/Android/iOS 一套 Flutter 代码）。
**架构核心：Filter Pipeline**——所有图像处理（几何/调整/滤镜/标注/尺寸/AI 指令）统一为
「Source → FilterNode 有序序列 → Sink」模型，节点参数即设计书 3.4 节操作栈 JSON，
坐标一律 0~1 相对比例。

- 设计书：`AgentImageViewer-设计文档-完整版v2.docx`（需求唯一来源）
- 开发流程：`开发流程.md`（S0~S7 阶段、验收口径）
- 仓库：https://github.com/Dashi929/AgentImageViewer （main，git 凭据已存可直接推）

## 2. 当前进度（截至交接）

| 里程碑 | 状态 |
|---|---|
| v0.1 MVP（浏览+编辑+图库+设置，Windows） | ✅ tag v0.1.0 |
| v0.2 AI Agent（七工具决议循环/串行队列/确认卡片/生成式） | ✅ |
| v0.3 系统集成（文件关联/单实例/回收站/MuiCache） | ✅（托盘/壁纸/开机启动未做） |
| v0.4 移动端（底栏/两列网格/手势/生命周期） | ✅ Android；iOS 未验证 |
| P1 增强（AI 流式/批量任务卡/图库四维度/快捷键/大图分级解码/Linux 构建） | ✅ |
| 幻灯片 + 自由旋转 | ✅ |
| HEIC 引导 / 生成式改图 / AI 上下文联动 / Release 三平台工作流 | ✅ |
| 回收站 → 「从图库删除」/ 旋转偏移修复 / 编辑历史改为退出即弃（2026-09-06 用户反馈） | ✅ |

**质量基线：132 项测试全绿；flutter analyze 零告警；Windows/Android 构建通过。**
每次提交前必须维持该基线。

> 2026-09-06 功能裁决（用户指示）：**回收站功能整体移除**（PowerShell 送删在用户环境
> 报「移入回收站失败」，不再修），图库/浏览改为「从图库删除」——library.json 记录
> hidden 列表，重扫不收录，**本地文件一律不动**。

## 3. 目录速览

```
lib/
├── main.dart               # 启动、窗口、单实例、关窗确认、appMain(集成测试复用)
├── app_state.dart          # AppState(InheritedNotifier)、aiBusy、forTest 工厂
├── core/                   # 纯 Dart 核心（可单测；pipeline 外可 import flutter/foundation）
│   ├── pipeline/
│   │   ├── node.dart       # FilterNode + 注册表 + 参数校验（新增节点必登记）
│   │   ├── pipeline.dart   # 节点序列 + 历史栈(撤销/重做/重置)
│   │   ├── render.dart     # 离屏合成(导出用) + paintAnnotateVectors + adjustColorFilter
│   │   │   └── mergedAdjustOf / presetExpansion / sizeAfter
│   │   ├── preview_painter.dart  # 编辑器画布屏幕直绘(CustomPainter)
│   │   └── source.dart     # thumbCacheKey(v2)、TileRect、LruCache
│   ├── image/              # image_manager(字节预算LRU+autoPin+软件位图重建)、exif、heic_support
│   ├── editor/             # editor_controller(generation 驱动画布重绘; active 注册表供测试)
│   ├── viewer/             # viewer_state(缩放平移) + slideshow(纯逻辑)
│   ├── scanner.dart        # 递归扫描/自然排序/GallerySort/groupByMonth
│   ├── db/                 # json_store(原子写)、library(索引+虚拟操作)、settings
│   └── ai/                 # ai_client(ChatBackend 抽象+SSE chatStream)、agent_session、
│                           # agent_tools(表2-4七工具)、generative(images/edits)、queue、vision_tagger
├── ui/                     # app_shell(层级:编辑>浏览>外壳)、gallery、viewer、editor、ai、settings
├── platform/               # file_assoc(HKCU 四层注册)（trash 已随回收站功能移除）
integration_test/           # 真机集成测试(编辑器标注全流程)
test/                       # 122 项单测（20 个文件）
tool/                       # push.sh(非交互推送)、mock_ai_server、gen_bench_images、register_assoc
dist/                       # Windows 便携包（gitignored）
.github/workflows/          # ci.yml(analyze+test 双平台)、release.yml(tag 触发三平台构建)
```

## 4. 关键机制与不变量（改代码前必读）

1. **autoPin**：`ImageManager.decode(..., autoPin: true)` 在**入缓存前**注册 pin，
   防止「插入即被预算淘汰释放」。所有**显示持有方**（viewer/editor/图库卡/缩略图条）
   必须成对 unpin（dispose 时）。乱序会导致位图被 dispose → 画布黑 + 渲染线程挂死。
2. **编辑器源图必须是 software 位图**：解码器产出 GPU 位图，`CustomPaint.drawImage`
   画 GPU 位图在 Impeller(Windows/Android) 上**灰/黑画布**。editor_page 用
   `ImageManager.toSoftwareImage` 重建后再交给 controller。浏览/图库用 RawImage 不受影响。
3. **编辑器画布 = 屏幕直绘**（EditorPreviewPainter），**禁止**恢复「离屏合成位图再
   RawImage 显示」的旧架构（曾致 toImage 崩溃/黑屏/toImageSync 黑屏三连）。
   离屏**整幅**合成只允许出现在**导出**（renderPipeline，单次）。唯一例外：马赛克
   预览的**块网格小图**（gw×gh ≤ 马赛克块数、百像素级 toImageSync，与导出
   _applyMosaic 同机制，拖拽中每帧重建一次），整幅中间位图仍然禁止。
4. **paintAnnotateVectors**：涂鸦节点只有 `points`、无 x/y —— 该函数已做分支容错；
   新增标注类型时**不得**在 doodle 分支前无条件读取 params['x']（曾致每帧 Null
   转型异常风暴 = 用户看到的「UI 卡死」）。
5. **颜色调整/滤镜走 widget 层 `ColorFiltered`**（editor_page）：Impeller 下
   `drawImage + ColorFilter` 组合不生效，画布 canvas 里加 filter 无效。
6. **节点参数校验**：新节点在 `node.dart` defaultRegistry 注册校验器，
   `render.dart` sizeAfter 补尺寸推演，`preview_painter` 补屏幕直绘分支（三处缺一不可）。
7. **解码器陷阱**：`instantiateImageCodec` 同时给 targetWidth+targetHeight 会
   **强制精确尺寸（非等比变形）**——只传一个维度。target 大于原图的 upscale 亦不可靠。
8. **编辑历史不持久化**（用户明确要求）：EditorController 只存内存，退出编辑即丢弃，
   需要保留结果走导出。`loadStack/saveStack` 已删除，JsonStore.saveEditStack 不再被
   调用——不要接回去；历史 `edits/<imageId>.json` 残留文件为惰性数据可无视。
9. **预览几何变换链**（preview_painter）：以 `nodeGeometryMatrix` 构建「源像素 →
   输出画布」内容映射矩阵（post-concat，按节点逆序乘各节点正向映射），源图原生尺寸
   `canvas.transform(geo.storage)` 后 1:1 绘制；禁止恢复「绕旧中心旋转 / 把源图拉伸进
   已换宽高的目标矩形」写法（曾致旋转后图片错位变形）。crop 的映射是**纯平移**（导出
   1:1 取样）——曾多乘 scale(1/rw) 致预览裁剪区放大错位（纯色测试察觉不了，四色条测试
   才抓到）。90° 倍数旋转仅 90/270 交换宽高（180 不交换），sizeAfter 与预览必须同步。
   导出 flip 节点必须「先 translate 后 scale」（先 scale 会整体越出画布）。
   注意 vector_math 的 `Matrix4.translate/scale` 首参为 dynamic：传 **int 字面量**会
   UnimplementedError（`x is double` 为 false），一律显式 `.0`；新 SDK 已弃用这两个
   方法，用 translateByDouble/scaleByDouble（第 4 参恒 1.0）。
10. **自绘标题栏独占一行，不悬浮覆盖内容**（main.dart）：曾是 `Stack[内容, 标题栏]`，
    Stack 命中测试在顶层命中即截停 → 顶部 36px 内所有按钮（编辑器撤销/导出、浏览
    返回）鼠标永远点不到。现在 `Column[标题栏, Expanded(内容)]`，不得改回悬浮式。
11. **页面级快捷键必须显式 requestFocus**：根壳有 `Focus(autofocus)` 抢先持有焦点，
    页面自己的 `Focus(autofocus)` 挂载时竞态必败 → 编辑器 Ctrl+Z/Y/S、Esc，浏览页
    方向键/Del 全部收不到键。editor_page/viewer_page 已持有 FocusNode 并在
    postFrame 强制 requestFocus——新增全屏页面（如有）必须照做。
    根壳全局键（Ctrl+K/?）靠祖先链收键，不受影响。

12. **标注绘制的坐标系契约**（preview_painter / editor_page）：标注矢量（含马赛克）
    必须画在**输出画布坐标系**（`translate(off)+scale(fit)` 已应用的上下文），
    restore 只能撤几何变换层、不得撤 fit/off 层——曾整层 restore 回控件坐标系，
    标注按输出像素尺度直绘，落点整体向右下飞出（拖拽覆盖层与提交预览错位）。
    **拖拽预览必须与提交渲染完全同路径**：_AnnotateDragPainter 对矩形/椭圆/箭头/
    涂鸦在输出画布坐标系调 paintAnnotateVectors（同一函数、同一默认参数）、并包进
    与提交层**同一个 ColorFiltered**（否则有调整时拖拽颜色 ≠ 提交颜色）；提交时
    把屏幕可见宽度换算进节点 `strokeWidth = max(2, 2/fit)` 输出像素（拖拽预览取
    同值）——曾固定 2 输出px，拖拽 2 控件px vs 提交 2×fit 控件px，粗细浓淡不一致。
    马赛克不走 paintAnnotateVectors：提交预览与拖拽实时预览共用 `paintMosaicRegion`，
    **与导出 _applyMosaic 同机制**——降采样到 gw×gh 块网格小图（toImageSync）再
    无插值放大回铺。**禁止**改回 compose「缩小→放大」图像滤镜方案：它在软件渲染
    （flutter_test）下正常、真实 GPU 后端（Windows/Android Impeller）实测完全不
    生效（2026-09-08 用户实测马赛克毫无效果，单测假绿）。块尺寸统一
    `mosaicBlockSize = min(边)/30 钳 8~120` 输出像素，预览/导出共用同一取值。
    **layer.toImage 陷阱**（Windows Impeller）：抓窗口帧栅格化带 DPR 缩放裁剪
    （内容放大 1.5×、与逻辑坐标错位），窗口帧逐像素断言不可用——像素级验证一律
    走 PictureRecorder 离屏栅格化（drag_commit_parity_test 是范例）。

## 5. 已知问题 / 待办（按优先级）

### P0（下个迭代建议）
- **MediaStore 相册接入**（Android）：当前只能扫应用可访问目录，系统相册需 MediaStore API
  （模拟器实测确认了沙盒限制，READ_MEDIA_IMAGES 已就绪）。
- **导出的 toImageSync 风险**：导出走离屏 renderPipeline（toImageSync+toByteData），
  安卓/Windows 实测通过，但不同 GPU 驱动下未全覆盖；若用户报导出挂死，候选方案：
  RepaintBoundary 截图或分块渲染。
  （注：马赛克预览的块网格小图同为 toImageSync，风险面相同，已随 2026-09-08 修复
  在 Windows 实测通过。）

### P1（设计书内未完成项）
- 幻灯片与编辑器在移动端布局已适配，但**编辑器文字标注的中文输入**在模拟器无法测
  （adb input text 不支持中文），需真机验证。
- WebP 导出（现静默降级 JPG）；画布扩展；编辑器清晰度滑杆（现为忽略+提示）。
- 设置项接线：cacheMB/thumbSize 已持久化但未注入 ImageManager/图库。
- 浏览视图「AI」入口（设计书 4.3.2 左侧栏；删除入口已落地：底栏按钮 + Del 键）。
- AI 会话持久化（重启丢失）。
- 图库文件夹树/收藏视图/时间轴（已有 chips 的 time 分组，缺独立视图）。

### P2（需外部资源）
- iOS 真机与上架（需 Mac+开发者账号）；Linux 桌面真机验证（Release 工作流已能出包）；
- NSIS 安装包；托盘/壁纸/开机启动（等用户拍板原生依赖）；
- 生成式改图的云端模型配置推广（当前默认留空禁用）。

## 6. 常用操作手册

```bash
# 质量基线（每次提交前）
flutter analyze && flutter test            # 132 项，确认稳定

# 运行
flutter run -d windows                     # 桌面调试（看实时日志排障首选）
flutter run -d <emulator-id>               # 安卓

# 推送（用户可能全屏占用时 GCM 弹窗会挂死，务必用脚本）
bash tool/push.sh                          # 内部走 credential fill + token 直推

# 性能基准（本地，不进 CI）
python tool/gen_bench_images.py && AIV_BENCH=1 flutter test test/bench_test.dart

# AI 全流程联调（无真实 key）
python tool/mock_ai_server.py              # 模拟器经 10.0.2.2:8000 访问
# 模拟器 app 设置: baseUrl=http://10.0.2.2:8000/v4, 任意 key
# settings.json 注入: adb shell "run-as <pkg> sh -c 'cat > files/data/settings.json'"

# Windows 打开方式注册（一次性验证）
dart run tool/register_assoc.dart          # 注册表四层写入，见 platform_test
```

### 模拟器实测 SOP（复现 UI 类 bug 的标准姿势）
1. `flutter build apk --debug` → `adb install -r` → 注入测试数据
   （`/data/data/<pkg>/files/` 下 library.json/settings.json/testpics，chown 对齐 app uid）。
2. **debug 包 + `adb logcat -d | grep flutter`** 是排障核心：Dart 异常/打印全部可见；
   Windows release 无日志，所以 Windows 排障改用 `flutter run -d windows --debug`。
3. 点击坐标换算：截图 920×2000(安卓)/1280×810(Win) 对应物理分辨率，先缩放再点；
   精确坐标用 `adb shell uiautomator dump`。
4. 修改后必须 `flutter build apk --debug && adb install -r` 才生效（debug 实例
   验证的是新包，注意别用旧包结论误判）。

## 7. 排障经验库（本轮实战沉淀，勿重蹈）

| 症状 | 根因 | 修复 |
|---|---|---|
| 画布空白+UI 卡死（编辑器） | 多个：LRU 淘汰释放显示中位图（autoPin 已修）；doodle 无 x/y 每帧 Null 转型异常风暴（已修）；离屏 toImage 崩溃（屏幕直绘已绕开） | 见 §4.1/4.4、render.dart |
| 画布灰/黑但标注矢量可见 | drawImage 绘制 GPU 位图进离屏/直绘画布失败（Impeller） | toSoftwareImage 重建 CPU 位图；或屏幕直绘 |
| 滤镜/调整点了没效果 | drawImage+ColorFilter 组合 Impeller 不生效 | widget 层 ColorFiltered |
| 图库缩略图变形 | codec 同时给 targetWidth+Height 强制精确尺寸（非等比） | 只传 targetWidth；缓存键 v2 |
| 图库整页黑屏 | 横向滚动内 Expanded = 无界约束崩溃 | SizedBox 定宽 |
| 双击缩放失灵 | onDoubleTap 与 scale 手势 arena 竞争 | 自实现双击检测（pointer up 判定） |
| 触屏唤不醒控件/手势只在图内有效 | MouseRegion 不含触摸；命中区非 opaque | Listener pointer down/move + HitTestBehavior.opaque |
| GCM 推送挂死 | 用户全屏应用时凭据 UI 弹不出 | tool/push.sh（token 直推）；卡住的 git-credential-manager 进程可 taskkill |
| settings.json 里 ai 配置丢失 | 覆盖安装偶发清数据（模拟器） | 重新注入；正式环境无此问题 |
| 编辑器旋转后图片错位/变形 | 预览绕「旋转前中心」旋转且把源图拉伸进已换宽高的目标矩形；180° 也被交换宽高 | 预览按节点逆序施加正向内容映射+原生尺寸绘制（见 §4.9）；sizeAfter/export 仅 90/270 交换；导出 flip-v 改先 translate 后 scale（原越出画布出空白图） |
| Del 键「移入回收站失败」 | PowerShell VisualBasic 送删在用户环境不可用 | 功能整体移除，改「从图库删除」（library.json hidden，文件不动） |
| 自动化工具查询窗口后 debug 实例秒退（日志 AXTree 刷屏 + Lost connection） | Flutter Windows 引擎 accessibility_bridge 已知缺陷，被无障碍树查询触发（get_app_state 类工具），与应用代码无关 | 排障 Flutter 窗口只用屏幕截图 + 原生事件点击（strategy=event），禁做无障碍树查询 |
| 用户反馈「打开/关闭软件变慢」（2026-09-07 排查） | 非应用问题：启动/关闭路径无阻塞点（library.json 仅 3KB、监控目录空、重扫本就后台）；当日晚间《赛博朋克2077》全屏占用 GPU/CPU，且自动化调试期间多个 flutter run/编译进程并发；Debug 构建（JIT+断言）本身启动就慢 | 日常用 dist 正式包（文件关联也已指向 dist）；排障结束清理 flutter/dart 进程；验证性能需在无重载场景另行计时 |
| 裁剪后「不能撤销」（2026-09-07 用户反馈，双根因） | ① 自绘标题栏悬浮覆盖顶部 36px，撤销按钮命中测试永远被拖拽区截停；② 焦点从未进入编辑器子树（根壳 autofocus 抢占），Ctrl+Z/Y/S/Esc 全部收不到。点击死按钮区落到 DragToMoveArea，按下微动即触发原生窗口拖拽模态循环，观感即「整个应用卡死」 | 标题栏改独立占位行（§4.10）+ editor/viewer 显式 requestFocus（§4.11）；集成测试 editor_crop_undo_test 双路径回归（严格命中测试 + 焦点断言） |
| 自由旋转「越转越少」+ 滑杆不即时生效（2026-09-07 用户反馈） | 每次应用旋转都对上一次的烘焙结果再扩包围盒（暗角累积、内容占比越来越小）；滑杆需点「应用」按钮才生效 | 滑杆拖动 → 控制器预览态（不入栈、generation++ 驱动重绘），松手提交并与栈尾同类节点合并（free_rotate 合并角度归一化；adjust 提交会话增量）。注意：滑杆值必须放 State 字段（notify 重建会把 build 局部变量重置回 0）；集成测试 editor_slider_live_test 回归 |
| 裁剪无预览直接生效 / 比例预设无效（2026-09-07 用户反馈） | 比例按钮挂在从未接线的 `_cropKey`（currentContext 恒为 null）上，点了没反应；拖拽直接入栈无确认 | 裁剪改两阶段：「自由裁剪」按钮为手动入口（画布拖拽只更新选区），比例按钮在当前画幅中心生成最大居中选区；选区渲染 PS 式预览（外压暗+三分线），「应用裁剪 (Enter)」确认入栈、「取消 (Esc)」丢弃，Esc 优先取消选区再退出编辑。集成测试 editor_crop_session_test 回归 |
| 标注拖动全是矩形框，无法区分类型（2026-09-07 用户反馈） | 所有标注共用一个矩形拖拽覆盖层 | _AnnotateDragPainter 按类型渲染预览（与 paintAnnotateVectors 已提交视觉一致）；文字标注拖出虚线框即文字框（锚点=框左上角、字号=框高换算）。集成测试 editor_annotate_preview_test 六类型回归 |
| 标注「拖动位置与最终生成位置不一样」（2026-09-07 用户反馈，双根因） | ① onPanStart 在手势竞争裁决（越过 touch slop）时才触发，localPosition 是裁决时事件位置而非按下位置，起点沿拖动方向漂移；② **_applyAnnotate 导出从不画底图**——导出结果是「透明画布+标注块」（长期 bug，标注导出此前从未被像素级验证） | ① 画布用 Listener.onPointerDown 锚定真实按下位置，onPanStart 用它替代自身 localPosition；② _applyAnnotate 先 drawImage 底图再叠加矢量。editor_annot_export_pos_test 像素级回归：拖马赛克到 rel(0.2,0.25)-(0.6,0.65)，导出图压暗区域包围盒必须逐边吻合 |
| 文字标注永远落在图像右下角（2026-09-07 用户反馈「编辑时位置已不对」） | 新版文字框提交把**像素坐标**直接 `clamp(0,1)` 当相对比例用——几百 px 全部钳成 1，锚点恒为 (1,1)（editor_text_pos_test 像素级捕获） | 换算必须走 rx()/ry()（÷画布尺寸）；提交参数打印 + 导出红色像素包围盒双重断言 |
| GIF 打开就快放、帧速飞快（2026-09-07 用户反馈） | ① 主根因：Flutter 引擎对 GIF 帧延时**原样返回**不做钳制（手工构造 0/1/2/5cs GIF 实测引擎报 [0,10,20,50]ms，见 gif_frame_duration_test 探针），浏览页 `Timer(info.duration)` 遇 0 延时帧（GIF 中极常见）零间隔空转，动图快放到不可看；② 双倍速隐患：暂停/恢复竞态可产生**两个并发播放循环**（取帧在途时恢复不校验在途状态、旧 timer 未掐又起新循环） | ① 新增顶层 `clampAnimFrameDelay`：≤10ms 钳 100ms（Chrome/Firefox 惯例，gif_frame_duration_test 单测）；② 播放循环换代号 `_animGen`，外部启动一律走 `_startAnimLoop`（先 cancel 旧 timer 再 ++gen），在途取帧完成后按 codec/暂停/代号三重校验不符即丢弃；③ 暂停拆 `_userPaused`/`_autoPaused` 两源（顺带修复 hidden→paused 连续触发覆盖标记导致退后台后 GIF 永不续播）；④ 动图跳过静态分级升级（`_maybeUpgrade` 查 `_animCodec`），防静态帧闪回覆盖动画帧；⑤ `_maybeAnimate` 换图时 dispose 旧 codec（原先泄漏） |
| 马赛克/涂鸦标注「提交后落点偏移」＋马赛克只是半透明黑块＋裁剪预览区放大错位（2026-09-08 用户反馈，三根因同在预览绘制） | ① 99b79b8 旋转重构把 `canvas.save()` 挪到 translate(off)+scale(fit) 之前，restore 后标注画在**控件坐标系**（缺 fit 缩放+off 平移，按输出像素尺度直绘→落点向右下飞出，仅 fit=1 时不可见，故纯 fit=1 的旧测试没抓到）；② 马赛克预览/导出都只画半透明黑块（注释声称「导出精确像素化」但从未实现）；③ 同次重构 crop 内容映射多乘 scale(1/rw)，预览裁剪区放大 1/rw（纯色测试无法察觉） | ① 标注上下文改为「输出画布坐标系」（嵌套 save：restore 只撤几何）；② 预览/拖拽共用 paintMosaicRegion（compose「缩小[双线性]→放大[无插值]」图像滤镜真实像素化，无离屏位图），导出 _applyMosaic 降采样块网格再无插值放大，块尺寸 min(边)/40 钳 6~96；③ crop 映射改纯平移。几何链整体改为 nodeGeometryMatrix 矩阵（预览/拖拽共用；Matrix4.translate/scale 首参 dynamic 传 int 字面量会 UnimplementedError，已弃用改 ByDouble）。回归：preview_geometry_test 三新测（fit≠1 标注位置/马赛克块内均匀/四色条 crop）+ render_test 导出马赛克像素化 |
| 标注「拖动时的颜色比实际应用的浅」＋马赛克效果完全不明显（2026-09-08 用户反馈，同日第二回合） | ① 拖拽覆盖层自绘 2 **控件**px 描边且在调色滤镜之外，提交后是 2 **输出**px（屏幕上 2×fit）且被 ColorFiltered 调色——上一回合只统一了几何位置，没统一渲染路径，fit≠1 或有调整时两侧粗细浓淡必然不同；② 上一回合引入的 compose 图像滤镜像素化**只在软件渲染（flutter_test）下生效**，Windows/Android Impeller 真机实测完全不生效——纯软件渲染单测假绿掩盖（用户看到马赛克拖拽/提交均无效果） | ① _AnnotateDragPainter 改为与提交渲染完全同路径（输出画布坐标系 + 同一 paintAnnotateVectors + 同一默认参数），并包进与提交层同一个 ColorFiltered；提交时把屏幕可见宽度换算进节点 strokeWidth = max(2, 2/fit)（顺带落实原 P0「涂鸦 2px 偏细」，拖拽预览取同值）；② paintMosaicRegion 改与导出 _applyMosaic 同机制：降采样到块网格小图（toImageSync，百像素级）→ 无插值放大回铺，块尺寸 min(边)/30 钳 8~120。回归三件套：mosaic_backend_pixel_test（真实引擎进程内直接像素断言 paintMosaicRegion，防软件渲染假绿）、drag_commit_parity_test（拖拽中组件树 painter 与提交后 EditorPreviewPainter 离屏逐像素对比，描边 bbox 1px 内重合）、editor_annot_export_pos_test 改黑白条纹图（纯色图真实像素化差异恒为零，旧「压暗区域」断言是假实现时代产物）。另记录：Windows Impeller 下 layer.toImage 抓窗口帧带 DPR 缩放错位（§4.12），窗口帧断言不可用 |

## 8. 验证矩阵（哪些平台验证过什么）

- Windows（本机）：全功能+涂鸦回归+导出+文件关联真实注册表测试 ✅
- Windows（2026-09-08）：标注六类型拖拽流程集成测试（含马赛克像素化预览/坐标契约修复后）✅
- Windows（2026-09-08 第二回合）：马赛克 compose→块网格小图重构后，8 项集成测试
  （六类型拖拽/马赛克后端像素化/拖拽-提交逐像素一致性/导出位置条纹图）全绿 ✅；
  Android 待真机回归（Impeller 同为默认后端，compose 失效应同样存在）
- 安卓模拟器 avp_test(API 35, x86_64)：图库/浏览/编辑标注全家/滤镜/导出 ✅（debug）
- Linux：仅 CI 构建 ✅（UI 未人工验证）
- iOS：仅工程就绪，从未编译 ❌

## 9. 给下个 agent 的建议

1. **先跑基线**：`flutter analyze && flutter test`（应零告警 + 132 通过），
   不绿先修环境再动手。
2. **读 §4 不变量**——本项目大多数致命 bug 都源于违反其中一条。
3. **改 UI 前先在真机/模拟器实测**——Impeller 后端多个「理论正确但实际黑屏/崩溃」
   的坑都已踩过并记录在 §7，新绘制相关代码必须过模拟器实测。
4. **用户不常在电脑前**：自主推进，能修的自修；需要决策的汇总记录（放本文档 §10），
   最后统一汇报。授权规则：GCM/账户弹窗自动选 **Dashi929**。
5. 提交信息用中文、按里程碑/问题分节；推送一律走 `tool/push.sh`。

## 10. 待用户决策事项（累计未决）

1. 仓库可见性（现 public）；
2. iOS 上架与 Mac 环境；
3. Linux 桌面人工验收；
4. 托盘/壁纸/开机启动的原生依赖选型（system_tray 等）；
5. 生成式改图的云服务商与模型推广范围（现仅设置页可配，默认禁用）；
6. NSIS 安装包（需装 NSIS）。
